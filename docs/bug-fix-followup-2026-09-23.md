# 审查后续修复：2026-09-23

基线 `4422a84`。本次修复上一轮只读审查确认的九项问题；不表示全项目已经不存在其他缺陷。

| 优先级 | 问题                                                       | 修复                                                                                                            |
| ------ | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| P1     | 正常重启被退出监听器的 `stopped` 状态取消                  | 手动重启只通过 lifecycle 判断是否被后续 Stop 撤销                                                               |
| P1     | 120 条快照上限拆散一次操作，恢复途中逆向快照挤掉待恢复成员 | 项目适配层在完整操作结束后，统一保留两个存储中最近 120 个操作组；底层追加、加载和读取不再按单条截断             |
| P1     | 页面、卡片和对话重载的撤销状态不一致                       | 从恢复日志同步卡片状态，撤销前重新读取；两个入口共用忙状态；逆向快照持久化恢复深度，祖先淘汰后仍能判断撤销/重做 |
| P2     | 删除恢复记录遗漏隐藏的兄弟分支                             | 遍历并收集全部后代，删除使用完整分支集合                                                                        |
| P2     | 评测把多个工作表名拼成一个不存在的表名                     | 读取实际工作表名，区分真实含逗号的名称与元数据中的名称列表；未限定的列表地址采用官方评分的第一张表规则          |
| P2     | 评测拆坏含逗号的带引号表名                                 | 评分入口与越界检查共用地址解析器；支持转义单引号并保留官方对历史数据中多余引号的兼容行为                        |
| P2     | 参考文件部分保存后界面仍展示旧配置                         | 接收后端实际 `saved` 列表，并展示具体失败条目                                                                   |
| P2     | 初始化未结束时 Stop→Start 丢失新启动请求                   | 停止使旧初始化失效，新初始化独立运行；旧任务结束不能清空新的进行中任务                                          |
| P3     | 评测汇总漏计 `agent_status=None`                           | 字符串化状态后使用同一种类型计数                                                                                |

Pi 的变更位于 `scripts/pi-context-patches.mjs`，已从锁定、干净的上游源码重新生成 vendor。评分仍复用 SpreadsheetBench 的 `cell_level_compare`；仅替换地址拆分及工作簿打开/关闭的适配，不更改单元格判分规则。

复核中追加的一项：

| 优先级 | 问题                                                                                                                                             | 修复                                                                                                                                                       |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P2     | 升级前留下的逆向快照没有 `restoreDepth`，被当作深度 0：在旧链上"撤销→重做"后新快照记为深度 1，页面和卡片显示"已撤销"，再点会把已恢复的修改又撤掉 | 生成逆向快照时，源快照缺少深度就沿 `restoredFromSnapshotId` 回溯计算；折叠时，链根就是原始修改的行直接按链接深度判断，只有祖先已被淘汰的行才使用存储的深度 |

验证：

- Node 全套 220/220，通过（含追加项的 3 条回归，均在修复前失败）。
- Python 评测单元测试 15/15，通过。
- 前端生产构建通过。
- 恢复回归使用真实 Pi 日志与持久化编解码，覆盖跨两种存储的分组、121 条底层快照、恢复过程及整组淘汰。
- 页面与卡片回归覆盖交替撤销/重做，以及重载对话后的实际模拟单元格值。
- 400 条数据集地址字段离线解析通过；`60-7`、`130-9` 的目标工作表存在，参考答案自比较通过官方单元格比较器。
- 全部 400 题以 golden 对比 initial，新判分与官方 `compare_workbooks` 结果：398 题一致、0 题不同；官方在 `130-9`（表名含逗号）和 `283-32` 上抛异常，新判分修好前者，后者是整列地址 `A:G`，官方 `generate_cell_names` 本身不支持，新旧都失败，属原有限制。

边界：没有启动模型评测，没有操作真实 Excel 工作簿。已经被旧代码淘汰的快照内容无法补回。使本地更新生效需重启应用并重新加载任务窗格；旧进程中的重启逻辑有缺陷，首次更新应退出应用后重新启动。
