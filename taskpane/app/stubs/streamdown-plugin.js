// Stand-in for @streamdown/code, /math and /mermaid: streamdown treats a missing
// plugin as turned off, so fenced code renders plainly and math/mermaid stay text.
export const code = undefined;
export const math = undefined;
export const mermaid = undefined;
