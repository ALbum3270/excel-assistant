node_modules/
package-lock.json
dist/
*.png
*.ico
# Office manifests — prettier has no XML parser; leave them byte-stable
# (Office is picky and they're hand-maintained).
manifests/
# Demo-workspace CLAUDE.md files are managed by the daemon at runtime
# (it appends the CONTEXT-FILES block). Prettier must not reformat them
# or the committed form would drift from what the daemon writes, and
# running the demo would dirty the repo again.
examples/*/CLAUDE.md

# Local-only scratch (also gitignored).
PORT_TO_OFFICE_ADDINS.md
AGENTS.md
# Vendored AI Elements / shadcn-ui copies — kept byte-identical to upstream (plus patches).
taskpane/app/vendor/

# The other vendored copies, written by scripts/vendor-*.mjs and marked
# "do not edit by hand": reformatting them would make the committed file
# differ from what re-running the vendor script produces.
taskpane/shared/vendor/
daemon/vendor/
